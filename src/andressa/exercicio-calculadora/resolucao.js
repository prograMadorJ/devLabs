const num1 = 1;
const num2 = 2;
const num3 = 5;
const num4 = 6;

resultSoma = num1 + num2 + num3 + num4;


function calculadora (x, y, texto) {
    const soma = x + y;
    console.log(`${soma} ${texto} soma`);

    const divisao = x / y;
    console.log(`${divisao}  ${texto} divisao`);

    const multiplicacao = x * y;
    console.log(`${multiplicacao} ${texto} multiplicacao`);

    const subtracao = x - y;
    console.log(`${subtracao}  ${texto} subtracao`)
};




 