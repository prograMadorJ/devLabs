const num1 = 1;
const num2 = 2;
const num3 = 5;
const num4 = 6;

resultSoma = num1 + num2 + num3 + num4;


function calculadora (x, y) {
    const soma = x + y;
    console.log(`${soma} é o resultado da sua soma`);

    const divisao = x / y;
    console.log(`${divisao} é o resultado da sua divisão`);

    const multiplicacao = x * y;
    console.log(`${multiplicacao} é o resultado da sua multiplicação`);

    const subtracao = x - y;
    console.log(`${subtracao} é o resultado da sua subtração`)
};


calculadora(30,20)
 